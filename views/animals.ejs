<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head'); %>
  </head>
  <body class="container">
    <header><%- include('./partials/header'); %></header>

    <main>
      <div class="jumbotron">
        <h1>This is great</h1>
        <ul>
          <% availablePets.forEach((pet)=> { %>
          <li>
            <a class="text-decoration-none" href="/animals/<%= petType %>/<%= pet.name %>"
              ><%= pet.name %>
            </a>
          </li>
          <% }); %>
        </ul>
      </div>
    </main>

    <footer><%- include('./partials/footer'); %></footer>
  </body>
</html>
